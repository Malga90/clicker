import styled from "styled-components";

const DashboardContainer = styled.ul`
  list-style-type: none;
  text-transform: uppercase;
  color: red;
  font-weight: 700;
  padding: 0;
  margin-top: 50px;
`;

export default DashboardContainer;
