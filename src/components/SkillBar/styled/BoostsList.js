import styled from "styled-components";

const BoostsList = styled.ul`
  list-style-type: none;
  text-transform: uppercase;
  color: grey;
  font-weight: 700;
  padding: 0;
  margin-top: 50px;
`;

export default BoostsList;
